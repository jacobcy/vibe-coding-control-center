# Vibe Coding Configuration Template
# Copy this file to ~/.vibe/keys.env and fill in your values.
# DO NOT commit keys.env to version control!

# ================= Vibe Project Configuration =================
# Execution Tool: Defines which CLI tool to use for AI commands (vibe chat, vibe flow, etc.)
# Supported tools: opencode (free), claude (powerful), or codex (legacy)
# Each tool requires its corresponding CLI to be installed and available on PATH.
# You can override this with environment variable: export VIBE_DEFAULT_TOOL=claude
VIBE_DEFAULT_TOOL=opencode

# Default OpenCode model (used when VIBE_DEFAULT_TOOL=opencode)
# Popular free models: opencode/kimi-k2.5-free, opencode/deepseek-chat
VIBE_OPENCODE_MODEL=opencode/kimi-k2.5-free

# Tmux session name for vibe workspace (used by worktree/tmux workflow)
VIBE_SESSION=vibe

# ================= Agent Identity Configuration =================
# Git Identity: Defines the user.name and user.email used by 'vibe sign'
# This allows AI tools to have a distinct identity in change/commit history.
# If not set, it defaults to the value of VIBE_DEFAULT_TOOL.
VIBE_AGENT="claude"

# Editor Configuration: Defines which editor to use for 'vibe config edit' etc.
# Examples: code, trae, vim, nano
VIBE_EDITOR="code"

# ================= Claude Code Configuration =================

# API Keys and Endpoints
# ANTHROPIC_AUTH_TOKEN=<your-auth-token-here>

# API endpoint (default: https://api.anthropic.com)
# For China users: https://api.myprovider.com  # Replace with your own proxy provider
# Our scripts provide aliases to switch: c_cn (China) / c_off (Official)
# ANTHROPIC_BASE_URL=https://api.anthropic.com

# Model name (default: claude-sonnet-4-5)
# Available models: claude-sonnet-4-5, claude-opus-4-5
# ANTHROPIC_MODEL=claude-sonnet-4-5
# ANTHROPIC_SMALL_FAST_MODEL="claude-3-5-haiku-20241022"

# 默认模型配置（必需）：定义 Haiku / Sonnet / Opus 三个速度档位对应的模型
# ANTHROPIC_DEFAULT_HAIKU_MODEL="claude-haiku-4-5"   # 快速模型
# ANTHROPIC_DEFAULT_SONNET_MODEL="claude-sonnet-4-5" # 平衡模型
# ANTHROPIC_DEFAULT_OPUS_MODEL="claude-opus-4-5"     # 强力模型

# ================= MCP Servers Configuration (Optional) =================
# These are used by MCP (Model Context Protocol) servers, not by our scripts
# We configure MCP servers during installation, but they use these keys directly

# GitHub - Get token from: https://github.com/settings/tokens
# Permissions needed: repo, read:org
GITHUB_PERSONAL_ACCESS_TOKEN=<your-github-token-here>

# Brave Search API - Get key from: https://brave.com/search/api/
BRAVE_API_KEY=<your-brave-api-key-here>

# ================= Additional AI Models Configuration (Optional) =================
# These are used by external AI tools/services, not by our scripts directly

# Google Generative AI - Get key from: https://makersuite.google.com/app/apikey
GOOGLE_GENERATIVE_AI_API_KEY=<your-google-api-key-here>

# DeepSeek - Get key from: https://platform.deepseek.com/
# Note: OpenCode can use DeepSeek models without this key
DEEPSEEK_API_KEY=<your-deepseek-key-here>

# Moonshot - Get key from: https://platform.moonshot.cn/
MOONSHOT_API_KEY=<your-moonshot-key-here>
