#!/usr/bin/env zsh
set -e
SCRIPT_DIR="$(cd "$(dirname "$(realpath "$0")")" && pwd)"
VIBE_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"
if [[ -n "${1:-}" ]]; then
    exec zsh "${VIBE_ROOT}/scripts/env-manager.sh" keys "$@"
else
    exec zsh "${VIBE_ROOT}/scripts/env-manager.sh" keys
fi
