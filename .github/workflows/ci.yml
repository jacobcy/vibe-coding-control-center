name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    name: Lint & Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install bats-core
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y bats

      - name: Install shellcheck
        run: sudo apt-get install -y shellcheck

      - name: Install zsh
        run: sudo apt-get install -y zsh

      - name: Run dual-layer lint
        run: bash scripts/lint.sh

      - name: Run bats tests
        run: bats tests/

      - name: Check LOC ceiling (total ≤ 7000)
        run: |
          total=$(find lib/ bin/ -name '*.sh' -o -name 'vibe' | xargs cat | wc -l)
          echo "Total LOC: $total"
          if [ "$total" -gt 7000 ]; then
            echo "FAIL: Total LOC $total exceeds 7000 limit"
            exit 1
          fi
          echo "✅ Total LOC: $total / 7000"

      - name: Check per-file ceiling (≤ 300 lines each)
        run: |
          failed=0
          for f in lib/*.sh bin/vibe; do
            lines=$(wc -l < "$f")
            if [ "$lines" -gt 300 ]; then
              echo "FAIL: $f has $lines lines (limit: 300)"
              failed=$((failed + 1))
            else
              echo "  ✅ $f: $lines lines"
            fi
          done
          if [ "$failed" -gt 0 ]; then
            echo "FAIL: $failed files exceed the 300-line limit"
            exit 1
          fi
          echo "✅ All files within 300-line limit"
