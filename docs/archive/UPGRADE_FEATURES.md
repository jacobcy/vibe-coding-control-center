# 升级功能说明

## 🎉 新增功能概览

本次更新为 Vibe Coding Control Center 添加了完整的版本检测、更新和配置管理功能。

## ✨ 主要改进

### 1. 版本检测功能

#### 新增工具函数 (lib/utils.sh)

- **`get_command_version(cmd, version_flag)`**: 获取命令的版本号
- **`version_equal(v1, v2)`**: 比较两个版本是否相等
- **`version_greater_than(v1, v2)`**: 判断 v1 是否大于 v2
- **`version_less_than(v1, v2)`**: 判断 v1 是否小于 v2

#### 使用示例

```bash
# 获取版本
CLAUDE_VERSION=$(get_command_version "claude" "--version")

# 版本比较
if version_greater_than "2.0.0" "1.9.9"; then
    echo "需要更新"
fi
```

### 2. 软件更新功能

#### Claude Code 更新 (install/install-claude.sh)

- ✅ 检测已安装版本
- ✅ 显示当前版本号
- ✅ 询问是否更新到最新版本
- ✅ 支持 macOS (brew) 和 Linux (npm) 更新
- ✅ 更新后显示新版本号

#### OpenCode 更新 (install/install-opencode.sh)

- ✅ 检测已安装版本
- ✅ 显示当前版本号
- ✅ 询问是否更新到最新版本
- ✅ 支持 macOS (brew) 和 Linux (curl) 更新
- ✅ 更新后显示新版本号

#### oh-my-opencode 管理

- ✅ 检测是否已安装
- ✅ 支持更新已安装的 oh-my-opencode
- ✅ 自动运行安装脚本

### 3. 配置合并功能

#### MCP 配置智能合并

**之前**: 直接覆盖 `~/.claude.json`,丢失用户自定义配置

**现在**:
- ✅ 检测现有配置文件
- ✅ 询问用户是否合并或替换
- ✅ 使用 `jq` 进行智能 JSON 合并(如果可用)
- ✅ 自动备份现有配置(带时间戳)
- ✅ 合并失败时回退到备份+替换模式

#### 配置合并示例

```bash
# 如果已有 MCP 配置
Do you want to merge with existing MCP configuration? (No = replace) [y/N]

# 选择 Yes: 合并配置,保留自定义服务器
# 选择 No: 备份旧配置,使用新配置
```

### 4. 增强的状态显示

#### 主控制中心 (scripts/vibecoding.sh)

**系统状态显示**:
```
SYSTEM STATUS:
✓ Claude Code    : Installed (v2.1.31)
✓ OpenCode       : Installed (v1.1.51)
! oh-my-opencode : Not installed
✓ Keys Config    : Found (keys.env)
✓ MCP Config     : Found (11 servers)
```

**Equip 菜单显示**:
```
>> EQUIPPING TOOLS (INSTALL/UPDATE) <<
1. Install/Update Claude Code (current: v2.1.31)
2. Install/Update OpenCode (current: v1.1.51)
3. Back
```

### 5. 新增辅助函数

#### 更新函数 (lib/utils.sh)

- **`update_via_brew(package)`**: 通过 Homebrew 更新软件包
- **`update_via_npm(package)`**: 通过 npm 更新全局包
- **`merge_json_configs(existing, new, output)`**: 合并 JSON 配置文件

## 📋 使用流程

### 首次安装

```bash
# 安装 Claude Code
./install/install-claude.sh

# 安装 OpenCode
./install/install-opencode.sh
```

### 更新已安装软件

```bash
# 方式 1: 使用控制中心
./scripts/vibecoding.sh
# 选择 "2) EQUIP"
# 选择要更新的工具

# 方式 2: 直接运行安装脚本
./install/install-claude.sh    # 会检测并询问是否更新
./install/install-opencode.sh  # 会检测并询问是否更新
```

### 更新流程示例

```
2/6 Check & Update Claude CLI
✓ Claude CLI already installed (version: 2.1.30)
Do you want to update Claude CLI to the latest version? [y/N] y
✓ Updating claude-code via Homebrew...
★ Updated from 2.1.30 to 2.1.31
```

## 🔧 技术细节

### 版本检测机制

1. 使用 `command -v` 检测命令是否存在
2. 执行 `cmd --version` 获取版本输出
3. 使用正则表达式提取版本号: `[0-9]+\.[0-9]+(\.[0-9]+)?`
4. 支持多种版本格式: `v1.2.3`, `1.2.3`, `version 1.2.3`

### 版本比较算法

- 按 `.` 分割版本号为数组
- 逐位比较数字大小
- 自动填充缺失位为 0
- 使用 `10#` 前缀确保十进制比较

### MCP 配置合并策略

1. **优先使用 jq**: 如果系统安装了 `jq`,使用 `jq -s '.[0] * .[1]'` 进行深度合并
2. **回退方案**: 如果 `jq` 不可用,备份现有配置并替换
3. **安全保障**: 所有操作前都会创建带时间戳的备份文件

## 🧪 测试

运行测试脚本验证所有功能:

```bash
# 测试新功能
./test_new_features.sh

# 测试状态显示
./test_status_display.sh
```

## 📝 注意事项

1. **更新需要确认**: 所有更新操作都需要用户确认,不会自动执行
2. **配置备份**: MCP 配置更新时会自动创建备份文件
3. **版本显示**: 如果无法获取版本号,会显示 "version unknown"
4. **jq 依赖**: 建议安装 `jq` 以获得更好的 JSON 合并体验

## 🎯 后续改进建议

- [ ] 添加版本更新检查(检测是否有新版本可用)
- [ ] 支持静默更新模式(用于 CI/CD)
- [ ] 添加版本回退功能
- [ ] 支持更多包管理器(apt, yum 等)
- [ ] 添加更新日志显示

