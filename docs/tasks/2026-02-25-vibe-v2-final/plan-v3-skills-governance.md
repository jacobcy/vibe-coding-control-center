# Vibe Skills 治理体系设计文档

> **日期**: 2026-02-25
> **状态**: 已批准
> **基础**: SOUL.md §2 Cognition First (认知优先)
> **触发**: 架构审计结果 — ❌ 推倒重来 (3.5/10)

---

## 1. 背景

### 1.1 审计发现

架构审计揭示了系统性问题：

| 指标 | 合理基线 | 实际值 | 膨胀倍数 |
|------|---------|--------|---------|
| 总代码行数 | 800-1,200 | 14,293 | ~15x |
| lib/ 模块数 | 3-4 | 21 | ~6x |
| 死函数数 | 0 | 30+ | ∞ |

**根因**: 没有机制在开发过程中问 "这个该不该做？" 和 "这个是否合规？"。规则存在（CLAUDE.md HARD RULES、.agent/rules/），但无人执行。

### 1.2 核心原则

基于 SOUL.md §2 Cognition First：

- **探索期**（认知未建立）：弱规范 — 唯一硬规范是 worktree 隔离
- **收敛期**（认知已建立）：强规范 — scope gate, LOC ceiling, PR compliance
- **阶段切换** = 架构审计 = 认知确认仪式

本设计文档描述的是**收敛期**的治理体系。

### 1.3 设计目标

1. **以 vibe flow 生命周期为骨架** — 所有技能编排进 start→spec→test→dev→review→pr→done
2. **以 PR 为唯一合规门** — 所有代码必须通过 PR，在 PR 过程中进行合规审核
3. **结合 worktree 模式** — 禁止 main 上提交，`vibe_main_guard` 强制执行
4. **统一现有+新建技能** — 建立完整的 Vibe Coding Skills 库

---

## 2. 架构概览

### 2.1 技能分类（3 层）

```
┌──────────────────────────────────────────────┐
│              流程技能 (Process)                │
│   save · continue · check                     │
│   保护认知资产、维持工作连续性                   │
├──────────────────────────────────────────────┤
│              守护技能 (Guardian)               │
│   scope-gate · boundary-check · rules-enforcer│
│   预防膨胀、边界检查、规则执行                   │
├──────────────────────────────────────────────┤
│              审计技能 (Audit)                  │
│   architecture-audit · drift-detector          │
│   全局评估、偏移检测                            │
└──────────────────────────────────────────────┘
```

### 2.2 技能清单（8 个）

| 技能 | 类别 | 位置 | 执行强度 | 状态 |
|------|------|------|---------|------|
| vibe-save | 流程 | 项目级 `.agent/skills/` | 手动 | ✅ 已有 |
| vibe-continue | 流程 | 项目级 `.agent/skills/` | 手动 | ✅ 已有 |
| vibe-check | 流程 | 项目级 `.agent/skills/` | 手动 | ✅ 已有 |
| vibe-scope-gate | 守护 | 项目级 `.agent/skills/` | 硬执行 | ✅ 已有 |
| vibe-boundary-check | 守护 | 项目级 `.agent/skills/` | 分级 | ✅ 已有 |
| vibe-rules-enforcer | 守护 | 项目级 `.agent/skills/` | 分级 | ✅ 已有 |
| vibe-audit | 审计 | 项目级 `.agent/skills/` | 手动 | ✅ 已有 |
| vibe-drift | 审计 | 项目级 `.agent/skills/` | 建议 | ✅ 已有 |

---

## 3. 工作流编排：vibe flow × Skills

### 3.1 生命周期映射

```
vibe flow start  ──→  scope-gate (新功能准入检查)
                       └─ "这个功能该不该做？SOUL.md 说了吗？"
     │
vibe flow spec   ──→  save (保存 PRD/spec)
     │
vibe flow test   ──→  boundary-check (测试规范检查)
     │
vibe flow dev    ──→  boundary-check (开发中边界检查)
                       └─ LOC ceiling, 函数长度, 死代码
     │
vibe flow review ──→  rules-enforcer (全量规则检查)
                       └─ 生成合规报告
     │
vibe flow pr     ──→  合规报告附加到 PR description
                       └─ PR = 唯一合规门
     │
vibe flow done   ──→  save (归档) + check (一致性验证)
```

### 3.2 Worktree 集成

```
wtnew <branch> [agent]  →  创建 wt-<agent>-<branch>
                            自动设置 git identity
                            vibe_main_guard 防护主分支

所有开发在 worktree 中进行
代码通过 PR 回到 main
PR 过程中触发合规审核
```

---

## 4. 新技能规格

### 4.1 scope-gate（范围守门员）

**目的**: 在 `vibe flow start` 时检查新功能是否在 SOUL.md 和 CLAUDE.md 定义的范围内。

**触发**: `vibe flow start` 自动触发，或手动调用。

**检查项**:
1. 功能是否在 CLAUDE.md "Core Identity: What We ARE" 范围内？
2. 功能是否在 CLAUDE.md "不做清单" (Scope Gate) 中被禁止？
3. 功能是否符合 SOUL.md 原则？
4. 预估 LOC 是否在 budget 内？

**输出**:
```
## Scope Gate 检查
✅ 在范围内 / ❌ 超出范围 / ⚠️ 边界模糊

理由: [one paragraph]
LOC 预算: 剩余 [X] 行（当前 [Y]/[Z]）
建议: [继续 / 需讨论 / 拒绝]
```

**执行强度**: 硬执行 — ❌ 结果则阻断 `vibe flow start`。

### 4.2 boundary-check（边界检查员）

**目的**: 在开发过程中定期检查代码指标是否在治理边界内。

**触发**: `vibe flow dev` / `vibe flow test` 自动触发，或手动调用。

**检查项**:
1. 总 LOC (lib/ + bin/) 是否 ≤ 上限？
2. 单文件是否 ≤ 200 行？
3. 单函数是否 ≤ 50 行？
4. 每个文件函数数是否 ≤ 15？
5. 有无新增死代码？（定义但未调用的函数）
6. 有无新增依赖？是否必要？

**输出**:
```
## Boundary Check
| 指标 | 上限 | 当前 | 状态 |
|------|------|------|------|
| 总 LOC | 1200 | XXX | ✅/⚠️/❌ |
| 最大文件 | 200 | XXX | ✅/⚠️/❌ |
| 死代码 | 0 | X个 | ✅/❌ |
```

**执行强度**: 分级 — 核心目录 (lib/, bin/) 硬执行，其他目录建议。

### 4.3 rules-enforcer（规则执行官）

**目的**: 在 `vibe flow review` 时执行全量合规检查，生成 PR 合规报告。

**触发**: `vibe flow review` 自动触发。

**检查链**:
1. boundary-check 全量指标
2. CLAUDE.md HARD RULES 逐条验证
3. .agent/rules/*.md 规则适用性检查
4. git 规范检查（commit message、branch naming）
5. 文档一致性（README/CLAUDE.md 是否与代码匹配）

**输出**: PR 合规报告（见 §5）

**执行强度**: 分级 — 生成报告但不阻断，由 PR reviewer 决策。

### 4.4 drift-detector（偏移探测器）

**目的**: 定期检测项目是否偏离初衷。

**触发**: 手动调用，或周期性审查时使用。

**检查项**:
1. CLAUDE.md 声称的身份 vs 代码实际功能
2. 功能增长趋势分析（git log）
3. 复杂度增长轨迹
4. 是否需要触发架构审计？

**输出**:
```
## Drift Detection
偏离度: [X%]
趋势: [收敛 / 发散 / 稳定]
建议: [继续 / 警告 / 触发审计]
```

**执行强度**: 建议 — 仅输出报告和建议。

---

## 5. PR 合规门

### 5.1 合规报告模板

每个 PR 必须附带由 rules-enforcer 生成的合规报告：

```markdown
## 📋 合规报告

### LOC Diff
Before: lib/+bin/ = XXX lines
After:  lib/+bin/ = YYY lines
Delta:  +/-ZZ lines

### HARD RULES 检查
- [x] Rule 1: LOC Ceiling (≤1,200) — 当前: XXX ✅
- [x] Rule 2: Single File Limit (≤200) — 最大: XXX ✅
- [x] Rule 3: Zero Dead Code — 死函数: 0 ✅
- [x] Rule 4: 不做清单 — 无违规 ✅
- [x] Rule 5: Tool First — 无自造轮子 ✅
- [x] Rule 6: New Feature Gate — SOUL.md 确认 ✅
- [x] Rule 7: PR LOC Diff — 已包含 ✅

### 边界检查
[boundary-check 输出]

### 审查建议
- [任何需要 reviewer 注意的事项]
```

### 5.2 门控规则

| 检查项 | 失败后果 |
|--------|---------|
| LOC 超上限 | 阻断合入 |
| 单文件超 200 行 | 阻断合入 |
| 新增死代码 | 阻断合入 |
| 违反不做清单 | 阻断合入 |
| LOC Diff 缺失 | 阻断合入 |
| 文档不一致 | 警告，不阻断 |
| 偏离度 >20% | 警告，触发审计 |

### 5.3 标签系统

| 标签 | 含义 |
|------|------|
| `compliance: pass` | 全部检查通过 |
| `compliance: warning` | 有警告但不阻断 |
| `compliance: fail` | 有阻断项，需修复 |
| `audit: required` | 偏离度触发审计需求 |

---

## 6. 配置中心：governance.yaml

```yaml
# .agent/governance.yaml
# Vibe Skills 治理配置

version: "1.0"
phase: convergence  # exploration | convergence

# 认知阶段 (SOUL.md §2)
cognition:
  phase: convergence
  last_audit: "2026-02-25"
  audit_verdict: "rebuild"

# LOC 预算
budgets:
  total: 1200          # lib/ + bin/ 总上限
  file_max: 200         # 单文件上限
  function_max: 50      # 单函数上限
  functions_per_file: 15 # 每文件函数数上限

# 执行强度
enforcement:
  core_dirs:            # 硬执行
    - lib/
    - bin/
  advisory_dirs:        # 建议
    - config/
    - scripts/
    - install/

# vibe flow 钩子
flow_hooks:
  start:
    - scope-gate
  dev:
    - boundary-check
  test:
    - boundary-check
  review:
    - rules-enforcer
  done:
    - save
    - check

# 阈值
thresholds:
  drift_warning: 20     # 偏离度警告线 (%)
  drift_audit: 30        # 偏离度审计线 (%)
  bloat_rebuild: 5       # 膨胀倍数 → 推倒重来
  dead_code_max: 0       # 死代码容忍 (个)
```

---

## 7. 技能标准 (SKILL_STANDARD.md)

所有技能必须遵循统一标准：

### 7.1 文件结构
```
skills/<name>/
  SKILL.md          # 技能定义（必须）
  README.md         # 使用说明（可选）
  examples/         # 示例（可选）
```

### 7.2 SKILL.md 必须包含

```yaml
---
name: <skill-name>
description: <one-line description>
category: process | guardian | audit
trigger: manual | auto | mixed
enforcement: hard | tiered | advisory
phase: exploration | convergence | both
---
```

### 7.3 必须章节
- **System Role**: 技能人格和硬规则
- **Overview**: 一段话描述目的
- **When to Use**: 触发条件列表
- **Execution Steps**: 具体步骤
- **Output Format**: 输出模板
- **What This Skill Does NOT Do**: 边界声明

---

## 8. 现有技能适配

### 8.1 save（保存技能）

**适配**: 无需大改。增加 flow hook 集成点。
- 在 `vibe flow done` 时自动调用
- 保存格式不变

### 8.2 continue（继续技能）

**适配**: 无需大改。
- 增加 governance 状态恢复（读取 governance.yaml phase）

### 8.3 check（检查技能）

**适配**: 轻度增强。
- 增加 governance.yaml 一致性检查
- 增加 flow state 与 memory 的交叉验证

### 8.4 architecture-audit（审计技能）

**适配**: 已更新。
- 新增 Phase 3: 开发者对话
- 降低推倒重来门槛（膨胀 >5x 直接建议）
- 增加资产提取清单
- 引用 SOUL.md §2 Cognition First

---

## 9. 实施路线图

### Batch 0（Week 0）: 基础设施
- [x] SOUL.md §2 Cognition First ← **已完成**
- [ ] `docs/standards/SKILL_STANDARD.md` — 技能标准规范
- [ ] `.agent/governance.yaml` — 治理配置
- [ ] CLAUDE.md 更新 — 引用治理体系

### Batch 1（Week 1）: 守护技能
- [ ] `scope-gate` — 范围守门员
- [ ] `boundary-check` — 边界检查员

### Batch 2（Week 2）: 执行与集成
- [ ] `rules-enforcer` — 规则执行官
- [ ] vibe flow hook 集成
- [ ] PR 合规报告生成

### Batch 3（Week 3-4）: 审计与闭环
- [ ] `drift-detector` — 偏移探测器
- [ ] 现有技能适配
- [ ] 集成测试

---

## 10. 风险缓解

| 风险 | 缓解措施 |
|------|---------|
| 治理本身成为膨胀源 | governance.yaml < 50行，所有技能是 Markdown，不增加 lib/ 代码 |
| 规范阻碍开发速度 | 双阶段模型 — 探索期弱规范，只在收敛期强执行 |
| 技能维护负担 | 技能是独立 Markdown 文件，无代码依赖，修改零风险 |
| flow hook 增加复杂度 | hook 只是"在这个阶段提醒 agent 调用技能"，不是代码级 hook |
| 开发者绕过门控 | PR 是唯一合入路径，GitHub Rulesets 强制执行 |

---

## 附录 A: 与 OpenSpec 的关系

本治理体系与 OpenSpec 工作流互补：
- OpenSpec 管理**变更流程**（new → continue → verify → archive）
- 治理体系管理**合规检查**（scope → boundary → rules → PR）
- 两者通过 vibe flow 生命周期集成

## 附录 B: 术语表

| 术语 | 定义 |
|------|------|
| 认知资产 | 开发者通过探索积累的心智模型和设计决策 |
| 探索期 | 认知未建立，代码是草稿纸的阶段 |
| 收敛期 | 认知已建立，目标明确的阶段 |
| 合规门 | PR 过程中的强制检查点 |
| 资产提取 | 从原型中识别可复用的文件、逻辑、决策 |
| 膨胀倍数 | 实际代码量 / 合理基线代码量 |
